<Window x:Class="RZDTrainer.Views.DeviceSelectorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Выбор микрофона" Height="350" Width="450"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="🎤 Выберите устройство для распознавания речи" 
                   FontSize="14" FontWeight="Bold" Margin="0,0,0,15"/>
        
        <TextBlock Grid.Row="1" Text="Доступные микрофоны:" Margin="0,0,0,5"/>
        
        <ListBox x:Name="DevicesListBox" Grid.Row="2" 
                 DisplayMemberPath="Name" 
                 SelectionMode="Single"
                 Margin="0,0,0,15"/>
        
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="TestButton" Content="🔊 Тест" Width="80" Margin="0,0,10,0" 
                    Click="TestButton_Click" Background="#FF9800" Foreground="White"/>
            <Button x:Name="OkButton" Content="OK" Width="80" Margin="0,0,10,0" 
                    IsDefault="True" Click="OkButton_Click" Background="#4CAF50" Foreground="White"/>
            <Button x:Name="CancelButton" Content="Отмена" Width="80" 
                    IsCancel="True" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>